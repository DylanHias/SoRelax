# Canonical URL of the deployed site — used for metadata, sitemap, OG tags.
# In production set this to https://sorelaxmassage.be
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ——— TinaCMS (M2) ———
# Provision at https://app.tina.io (free tier). Required at build time once
# the CMS is wired up. Leave empty during M1.
NEXT_PUBLIC_TINA_CLIENT_ID=
TINA_TOKEN=

# ——— iubenda (M3) ———
# Required when /privacy and /cookies should embed the iubenda policy.
# The cookie banner on the site also keys off these IDs.
NEXT_PUBLIC_IUBENDA_SITE_ID=
NEXT_PUBLIC_IUBENDA_COOKIE_POLICY_ID=

# ——— Salonized (M3) ———
# Widget URLs that Tanja gets from her Salonized account.
# Once TinaCMS is wired up (M2) these move into settings.json and can be edited
# from /admin. They live here temporarily so M1 still works without the CMS.
NEXT_PUBLIC_SALONIZED_WIDGET_URL=
NEXT_PUBLIC_SALONIZED_GIFTCARD_URL=

# ——— Social (M3) ———
NEXT_PUBLIC_INSTAGRAM_URL=
NEXT_PUBLIC_FACEBOOK_URL=

# ——— Contact form backend (M3 — server-only, never expose) ———
# Choose Resend + Cloudflare Worker (preferred) or Formspree.
RESEND_API_KEY=
CONTACT_FORM_TO=info@sorelaxmassage.be
